# Branding — Agent Skills Factory

Canonical assets live in `branding/`:

| File | Use |
|------|-----|
| `logo.png` | Full banner (mascot + wordmark) as provided |
| `icon.png` | Transparent yellow mascot tile — favicons, sidebar, chips |
| `wordmark.png` | Transparent full wordmark for dark headers |
| `brand.json` | Colors + naming |

UI fork: `/Users/clint/skill-factory-canvas` (OpenHands Agent Canvas, MIT; keep copyright notices, no OpenHands trademarks in product chrome).

Theme tokens to match the logo: background `#0b0e14` / `#050A18`, accent yellow `#F5D547`, accent blue `#5EC8F0`.
